# --- TEMP / SHELL ARTIFACTS ---
PY
fee=*
notional=*
slip=*
from
import

# --- CONFIG BACKUPS ---
*.bak
*.bak2

# Local config backups
config/*.bak*

# Local-only artifacts
/METTI_QUI_IL_PATH_DEL_DB
/hyperliquid-bot-src.tgz

# --- output generati ---
*.csv

# --- frammenti / output accidentali (nomi-file spezzati) ---
SELECT
FROM
WHERE
AND
LIMIT
ORDER
=
0
asset,*
direction,*
edge,*
pair_*
fees_*
profit_*
slippage_*
timestamp*
ts*
theoretical_*
realized_*
run_id,*
route,*

# --- sicurezza / segreti ---
.env

# --- python venv / cache ---
.venv/
__pycache__/
**/__pycache__/
*.pyc
*.pyo

# --- runtime (se vuoi tenerli solo su VM) ---
logs/
data/
2_PROMPT_DEFINITIVO_PER_CURSOR_EMOJI.md
